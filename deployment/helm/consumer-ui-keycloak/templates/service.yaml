apiVersion: v1
kind: Service
metadata:
  name: {{ include "consumer-ui-keycloak.fullname" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "consumer-ui-keycloak.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}    # host port
      targetPort: 80                      # container port
      protocol: TCP
      name: http
  selector:
    {{- include "consumer-ui-keycloak.selectorLabels" . | nindent 4 }}
